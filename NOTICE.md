# Notice

`/teardown` is an original implementation. No code or text is copied from the
projects below; what is borrowed is structure, and it is named here.

## latent-spaces/brag (MIT)

https://github.com/latent-spaces/brag

Borrowed, in structure only:

- a written rubric that must be answered before any judgement is formed
- hard gates between steps, each one a checkable condition
- a short list of "laws" that override the model's default agreeableness
- the split between the skill owning the reasoning and a separate tool owning
  the mechanical work

`brag` applies this to your own project, to produce a launch video.
`/teardown` applies it to someone else's repository, to produce a verdict.

No files, prompts, assets, or code from `brag` are redistributed here.
